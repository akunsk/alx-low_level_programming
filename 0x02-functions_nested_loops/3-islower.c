#include "main.h"

/**
 * main - _islower checks for lowercase character
 * Description: @c the character to check
 * Return: 1 if c is lowercase, 0 otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
